{
  "name": "react-calendar",
  "version": "0.1.0",
  "private": true,
  "license": "UNLICENSED",
  "dependencies": {
    "prop-types": "^15.7.2",
    "react": "^16.8.4",
    "react-dom": "^16.8.4",
    "react-scripts": "2.1.8",
    "roast": "*"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "npm run linter && react-scripts build",
    "deploy": "npm run build && roast deploy",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "linter": "standard --fix"
  },
  "husky": {
    "hooks": {
      "pre-commit": "npm run build"
    }
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ],
  "devDependencies": {
    "husky": "^1.3.1",
    "standard": "*"
  }
}
